## Under different UI scaling ratio

use `Utility.ModifyCoordinatesForUIScale(e.Cursor.ScreenPixels)` to turn cursor position from screen pixels to menu UI coordinates.
or `e.Cursor.GetScaledScreenPixels()` if in UI like HUD(out of menu)

## Under different game scaling ratio

## When playing with mouse and keyboard

## When playing with controllers
